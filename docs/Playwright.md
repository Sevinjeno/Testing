With Playwright:

        It opens a real browser

        It types in inputs

        It clicks buttons

        It waits for navigation

        It checks UI

        It tests full flow

        It tests your app like a real user.

